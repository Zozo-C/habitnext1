import DemoMainApp from '@/components/DemoMainApp';

export const metadata = {
  title: 'HabitNext Demo - Mock Data Preview',
  description: 'Preview the HabitNext app with mock data',
};

export default function DemoPage() {
  return <DemoMainApp />;
}
